{% form_theme form theme %}
<form action="{{ path('fos_user_group_new') }}" {{ form_enctype(form) }} method="POST">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'group.new.submit'|trans([], 'FOSUserBundle') }}" />
    </div>
</form>
